import React from 'react';
import { NavLink } from 'react-router-dom';

const Navigation = () => {

    return (
        <nav>
            <NavLink to='/'></NavLink>
        </nav>
    )
}

export default Navigation;
